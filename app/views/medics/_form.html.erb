<%= form_with(model: medic) do |form| %>
  <% if medic.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(medic.errors.count, "error") %> prohibited this medic from being saved:</h2>

      <ul>
        <% medic.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :specialty, style: "display: block" %>
    <%= form.text_field :specialty %>
  </div>

  <div className="field">
    <%= form.label :hospital %>
    <%= form.select :tenant_id, options_from_collection_for_select(@hospitals, 'id', 'name', medic.tenant_id), include_blank: true %>
  </div>

  <%= form.hidden_field :tenant_id, value: @hospitals.id %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
